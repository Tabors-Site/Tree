{
  "name": "treeos-place",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "place": "cd place && node plant.js",
    "start": "cd place && node plant.js",
    "dev": "cd place && SITE_DEV=true node plant.js",
    "build": "cd site && npm run build",
    "dev:site": "cd site && npm run dev",
    "postinstall": "cd place && npm install && cd ../site && npm install --no-optional && cd ../cli && npm install",
    "test": "node --test test/core.test.js"
  }
}
