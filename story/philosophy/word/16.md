# 16 — verb-ops become Words (rungs 2–3)

Continuity note for the verb-op-as-Word arc. Where it sits, what landed, what's next, and the
worked-example plan. (Picks up from 10.md's do-op word-fold cutover.)

## The ladder (two agents, one doctrine: "weight in the fact-bytes, not the build")

Three rungs of "everything reduces to Words," split by file so two agents work in parallel:

1. **do-op `.word` bodies escaping via `host:`** — dissolve into composed `see`/`do`. *Other agent.*
   Landed this session: form-portal (portal.word composes `do create-matter with {...}`), create-matter
   de-`skipAudit`'d, the **`_factParams` keystone** in do.js (an enriching op returns its fact params;
   the ONE dispatcher path lays the caller-attributed fact — no word emits its own Fact), and the
   **nested-param grammar** (`do <op> with { k: "lit", k2: $ref, nested: {…}, arr: [...] }`).
2. **verb-op-SETS (NAME_OPS, BE_OPS, seeOps) are JS Maps, never folded** — fold them. ***Mine. DONE.***
3. **the `.word` AUTHORS the op (even folded, the source is still JS)** — ***Mine. IN PROGRESS.***

## Rung 2 — DONE this session (all three verb-op sets folded)

All three verbs now dispatch from the word-fold; the Maps are demoted to registration buffers.
Mirrors the do-op fold. Each verb-namespaced (`name:` / `be:` / `see:`) so they never collide in the
one shared projection. New functions in `seed/present/word/wordStore.js`:

- **NAME** (closed, 5 ops) — `declareNameOpsToFold` / `resolveNameOpFromFold`, `kind:"nameop"`,
  seedFold only. Dispatch swap in `seed/ibp/verbs/name.js`. Guard: **verify-name-ops-fold 7/7**.
- **BE** (closed, 6 ops incl `truename`, carries a `bootstrap` flag) — `declareBeOpsToFold` /
  `resolveBeOpFromFold`, `kind:"beop"`, seedFold only. **6** `getBeOp` dispatch sites swapped in
  `seed/ibp/verbs/be.js` (106 generic + 425/477/559/660/720 special branches release/switch/death/
  truename/connect — all read only `.handler`). Guard: **verify-be-ops-fold 6/6**.
- **SEE** (OPEN registry — seed + `<ext>:<name>`) — `declareSeeOpsToFold` / `resolveSeeOpFromFold`,
  `kind:"seeop"`, seedFold **+ a boot-end pass in genesis.js** (the coverage check proved **7** see ops
  register after seedFold: llm-connections, http-stats, connections, mongo-stats, federation-status,
  llm-chain, and `harmony:neighbors` — an EXTENSION op). 2 dispatch swaps in `seed/ibp/verbs/see.js`.
  Guards: **verify-see-ops-fold 5/5** (coverage 26/26), **verify-see-live 14/0**.

wordFold.js `seedFold` now has nine shapes (concept/do-op/type/reducer/roleword + name/be/seeop).
Bootstrap knot solved by existing bedrock: I_AM's own genesis `name:declare` + `be:birth` are raw
`emitFact` in sprout.js (never the verb), so they predate + ground the fold untouched.
**Coordination:** the three resolvers never read `resolveDoOpFromFold` (separate kinds). genesis.js
touched additively (the SEE boot-end beside the existing do-op/type/roleword boot-end). Unstaged.

## Rung 3 — the `.word` authors the verb-op — DECISION: per-op `.word` (option C)

The fold made the op a Word the runtime READS; rung 3 makes the `.word` the thing the op is WRITTEN
as (the honor-vs-betray gap from the trace: today the op-SET source is still JS — `declareNameOpsToFold`
reads NAME_OPS). **Decided C, not a "can block"**, after reading the other agent's landed work
(portal.word / create-matter.word / grant-role.word / move.word — *portal.word is ground truth, the
others may be partly aspirational*). Every op there is a **per-op `.word`** with one shape:

```
When a being <does X>:
  see <floor-read>(...) as $var          # the floor (crypto/CAS/clock/fold), reached through SEE
  If <cond>, refuse "..." as <kind>       # gates — .word
  do <other-word> with { nested $refs }   # compose — words on words
  Return key: $var, factParams: $enriched # dispatcher stamps from factParams; the word never stamps
```

A "can block" (option A) is a half-measure: op-set in the Word, **logic still JS**. C makes a verb-op
the SAME kind of Word as a do-op → LLM-authorable, sealable, source-agnostic. Cost (accepted): the verb
dispatch (be.js/name.js) must **adopt `_factParams`** (stamp from a returned spec, like do.js) instead
of hardcoded `writeBeFact`/`writeNameFact`; and ~37 per-op `.word`s eventually. The crypto looks
irreducible but becomes a `see` floor-read (like `grant-stamp`/`resolve-birth-spec`).

## The worked example: `be:truename` FIRST

Mirror how form-portal proved rung 1 — one op, then the rest follow the template. **truename** is
chosen because it is the **most reducible**: re-pointing a being's trueName is morally
`do set-being with { field: "trueName", value: $name }` — a **composition**, like portal composes
create-matter. So it rides ONLY the portal-VERIFIED mechanism and needs **no new floor see-ops**. It
proves the genuinely new piece — the **BE dispatch adopting `_factParams`** (stamp from the returned
spec). The crypto-floor verb-ops (`name:declare` keypair mint, `be:connect` session unlock) come LATER,
once their floor-`see` pattern firms up on the other agent's side (don't build on the unverified part).

What truename needs: (a) **truename.word** — the control strand (see name-exists/not-banished floor-
reads, If/refuse gates, the trueName re-point, Return factParams); (b) **be.js `_factParams` adoption**;
(c) **declareBeOpsToFold reads truename.word** (per-op, mirroring `declareRoleWordsToFold` reading role
`.word`s); the BE_OPS truename JS handler retires. Fresh verifier **verify-truename-word.mjs** (the
existing truename-e2e is stale — `getRealityDomain` rename rot, 33 stale callers in .test/).

**LANDED + verified (2026-06-21): be:truename as a .word — the rung-3 pattern proven.**
Files: `seed/store/words/cherub/truename.word` (the control strand: 7 refuse gates + 4 floor see-ops +
`Return ... factParams: { trueName: $nameId }` — **Option B**, the word builds its own fact-params
object; confirmed parsing + endorsed by the other agent, who made the Return splitter brace-aware so
multi-key `factParams` works too); `truenameHost.js` (the 4 floor see-ops resolve-name-id /
resolve-target-being / name-exists / name-banished, wiring the same primitives be.js called, no
reimpl); `cherub/role.js` (`registerRoleWord("cherub","truename")`, truenameHandler → `_truenameViaWord`
shim mirroring `_createMatterViaWord` + the inert fallback, `cherubBeOps.truename.factAction`);
`be.js` (the truename branch swaps `writeBeFact` → the do.js-style dispatcher emit reading
`result._factParams`/`_factTarget`, `of.kind` hard-asserted being for BEING_ONLY_TARGET_VERBS);
`wordStore.js` (declareBeOpsToFold/resolveBeOpFromFold carry `factAction`/`skipAudit`). New guard
**verify-truename-word.mjs 6/6** — the proof: the emitted fact carries `params.trueName === resolved
nameId` + a `result.trued` sentinel (the inert fallback returns `granted`, not `trued`), so the WORD
authored the params and the dispatcher stamped. 11-verifier regression green (cherub/connect family +
all fold siblings). Unstaged.

NOTE — this is the SAFE-first cut: be.js STILL gates inline (the truename branch, ~be.js:605-664)
before the handler runs, so the .word's gates are reproduced-but-not-yet-live (be.js refuses first;
the .word's gates fire only on a clean miss). The .word is COMPLETE (ready to be the sole source). The
FULL cutover — delete be.js's inline validation so the .word's gates go live, moving that validation
into the handler's JS clean-miss fallback (mirroring createMatterHandler's JS body) — is the immediate
follow-on. Then BE's other reducible ops, then the crypto-floor ones (declare/connect), after their
floor-`see` pattern firms on the other agent's side.

## The endgame this serves (why it matters)

The Word layer becomes **source-agnostic**: a sealed `.word`, a form input, and a live LLM decode are
the same kind of thing — *ongoing Word composition → continuous evaluation → optional sealing into a
Fact*. The dispatcher owns the stamp, so an LLM-being (like a `.word`) composes freely but physically
cannot violate one-Fact-per-act / caller-attribution / content-addressing / presentism. form-portal
proved the sealed-file source; the LLM loop is the live source; verb-ops-as-Words closes the last
corner an LLM couldn't author. (See llm.md, language.md.)

## Constraints (persist across resets)
- NEVER `git commit` in this repo (leave staged/unstaged for Tabor/Sam). NEVER use "user" (being/name)
  or "substrate". No em/en dashes in chat.
- The floor (crypto, CAS, the one outside clock, the fold) legitimately stays `host:`, reached through
  `see`. A `host:` that escapes work that COULD be Words is debt against sealability.
- No `.word` emits its own Fact — `Return factParams: $x`, the dispatcher stamps once, caller-attributed.
- portal.word = ground truth; verify any claim about other `.word`s against the evaluator.
- Don't bump DESCRIPTOR_VERSION; no migration scaffolding (DB is wiped frequently).
