# The Word: flow IR contract (parser ⇄ evaluator)

The pilot (cherub) cut proves the straight-line case. The rich slices
(cherub-connect, governance-commons, grant-role, inheritation, matter-types,
subscription-schedule, NAME-declare) need real **flow structure**, and the parser
EMITS these IR nodes while the evaluator CONSUMES them — so they must agree on
one shape first. This file is that contract.

It was first drafted as §1–§5 (conditional / iteration / quantifier / refusal /
binding). A fan-out over all seven control-flow-bearing slices, with an
adversarial completeness critic, found that sketch missing the construct every
slice shares (the flow envelope) plus ~20 node kinds. This is the rebuilt,
complete version. Lanes: parser produces the IR (claude), evaluator runs it
(engine), the surface stays plain Word (Tabor's rule). Engine: refine the IR
where the evaluator needs it different; the **open questions** at the end are
yours to settle. Backlog: [phase5-coverage.md](./phase5-coverage.md).

## §0 — Flow envelope + trigger (THE construct all 7 slices share)

```
{ kind: "flow", when: <trigger>, binds: [<name>...], body: [<node>...] }
```
The body is an ORDERED array; §1–§13 nodes are its legal members (also legal in
any `then` / `else` / `foreach.body` / `case.body`). `<trigger>` is a tagged union:

| trigger | surface | shape |
|---|---|---|
| op | `When Cherub connects with a name and a password:` / `When I grant a role to another being:` | `{ kind:"op", verb:"be"|"do"|"name", op, actor?, alias?, receiver?, at?, params:[<name>...] }` — **binds** `params`, `actor` (and `alias`, e.g. `(the caller)`) into flow scope; downstream conds read them. |
| event | `When a steward closes a proposal:` (no `happens`) | `{ kind:"event", clause, external?:bool, timer?:bool, guard?:<cond> }` |
| external/timer | `When the tick comes due:` / `When the coalesce window closes:` | `{ kind:"event", external:true }` — engine wires to the tick loop, not a fact watch |
| state | `When it is dawn:` | `{ kind:"state", <var>:<value> }` (the existing wheel form) |
| quant | `When most members back a proposal:` | `{ kind:"quant", q, of, pred }` (see §4) |
| query | `When I ask whether a Name has authority over a being:` | `{ kind:"query", question, actor?, host? }` — a question-triggered flow whose body computes + returns |
| event+negated-guard | `When a being brings matter without naming its type:` | `{ kind:"event", clause, guard:{ ...negated } }` (absence-conditional) |

## §1 — Condition sub-language (the shared crux)

The parser CAPTURES, the evaluator RESOLVES. The parser lifts only structure
(connectives, negation), never interprets predicates (state-reads, equality,
existence — engine capabilities).

```
cond := <leaf> | { all: [cond...] } | { any: [cond...] }
<leaf> := { clause:"<verbatim>", negated:<bool>, subject?:"<noun phrase>" }
```
- `X and Y`, and multi-line juxtaposition under one `if`, → `all`. `X OR Y` → `any`.
- negation in a CONDITION: `not` / `no X` / parenthetical `(not remote)` is a gloss → `negated:true` (the parenthetical itself does not flip; the word `not` does). Distinct from negation on an ACT (§10).
- `the candidate is local`, `verification succeeds`, `the current trueName equals the caller's nameId`, `qualities.father` reads — all stay verbatim leaf clauses; the engine resolves them.

## §2 — Conditional (if / then / else)

```
{ kind: "if", cond: <cond>, then: [<node>...], else: [<node>...] }   // else optional
```
- Block: `If <cond>:` + indented body. Inline: `If <cond>, X, and Y.` → `then:[X,Y]` (a comma-conjoined **list**, not one act). Trailing: `<act>, if <cond>.` normalizes to the SAME node (`then:[<act>]`).
- `Otherwise:` / `Otherwise, <act>.` at the if's indent fills `else`.

## §3 — Iteration (foreach + break)

```
{ kind: "foreach", bind, in: <source>, ordered?:bool, all?:bool, body: [<node>...] }
{ kind: "break" }     // halts the nearest foreach
<source> := { ref } | { ref, filter:<cond> } | { walk:{ from, to, direction } }
```
- `For each candidate in order:` → `ordered:true`. `every schedule whose next fire time has passed` → `{ ref, filter }`. `walk the being-tree up from the being to the reality root` → `{ walk }`. `every subscription … is dropped` (cascade) → `all:true`.
- A flag set inside a foreach body **persists across iterations and after the loop** (accumulator), so `If no target could be inhabited` reads the OR over all passes (see §5).

## §4 — Quantifier (trigger or cond)

```
when: { kind:"quant", q, of:"<collection>", pred:"<clause>" }
cond: { quant: { q, of:<ref>, pred:"<clause>" } }
```
- `q ∈ every | most | all | no | any | some` (`any`/`some` = existential — `if any anchor matches` is the quantifier form of a §3 walk-and-break-on-first-match; `no` = the prohibition quantifier).
- **cadence-`every` is NOT this**: `every <number> <time-unit>` (`wake every N milliseconds`) is a trigger `cadence`/`interval` field, not a quant. Disambiguate lexically on the noun-phrase head (time unit → cadence; plural noun → quant).
- Firing: a quant `when` is **edge-triggered** on the predicate crossing false→true (the moment most have backed), NOT re-evaluated every turn. (Engine: confirm — open Q5.)

## §5 — Flag mark + coreference (the most pervasive pattern)

A self-directed state-mark with no explicit object sets a flow-local boolean a
SIBLING condition later reads.
```
{ kind: "mark", flag: "<canonicalName>", value: true }
```
- `the being is found` → `{mark, flag:"beingFound"}`; the sibling `If no being was found` / `If a being was found` resolves the **same** flag. `the being is owned`→`beingOwned`, `the target being can be inhabited`→`canInhabit`, `asFather is true`→`asFather`.
- The parser owns coreference: `inferFlag(clause)` is a **documented, deterministic** clause→flag function shared by mark-write and cond-read (open Q4).
- Disjunctive accumulation: a flag set in either branch of sibling guards, or in any foreach pass, is the OR (`asFather` true if any father-path succeeds).

## §6 — In-flow bind (ref OR act/host result)

```
{ kind: "bind", name, value: <ref> | { act: <act|host> } }
```
- `The driver trueName is the father's beingId.` → bind to a ref (the §5-original conditional-binding, used inside if-branches).
- `Cherub searches across all branches … (up to 5, capped)` producing `candidates` → bind to an act RESULT; that binding is what a later §3 `foreach in:{ref:"candidates"}` resolves. `classifyMatter(input) returns a ranked list` → bind to a host result. (A bind result is readable later in the same flow.)

## §7 — Effects (the world strand: acts that become facts)

```
{ kind: "refuse", message }                                   // error halt (§4-of-v1)
{ kind: "return", values:[<name>...], extra?:{ <k>:<literal|{ref}> } }  // SUCCESS terminator, ≠ refuse
{ kind: "derive", fact:{ type:"<verb>:<op>" }, reel?, attributedTo?, stored?:bool, when?:<cond>, infer?, symmetric?:bool }
{ kind: "append", collection:"<owner.field>", value:<ref> }   // durable; read by LATER flows (≠ §6 bind, transient)
{ kind: "assemble", target, fields:[{ name, value?, oneOf?:[...] }...] }  // record build, disjunctive field
{ kind: "notify", recipients:{ role?, ref?, all?:true }, carrying?:<ref> }
{ kind: "transition", subject, becomes:{ role?, state? } }    // "the visitor becomes a member"
{ kind: "host", builtin, args?, bind?, desc? }                // escape hatch; may bind its return
{ kind: "act", verb, op?, by, of?, to?, reflexive?:bool, negated?:bool, purpose?, keyedBy? }  // the plain act
```
- `return` carries named values + literal kv (`owned:true`) + flag-ref kv (`asFather:(true/false)`). It hands values to the transport (host-to-transport bridging).
- `derive` is the general fact-emission: `the grant lands as a fact on the position's reel, attributed to I` → `{derive, fact, reel, attributedTo:"I"}`. `becomes a fact: be:birth` → `{derive, fact:{type:"be:birth"}}`. `no point is stored; the walk grants coverage` → `stored:false`. `When X is in Y, Y contains X` → `{derive, when, infer:{relation:"contains"}, symmetric:true}`. The latest-wins liveness rule → `derive` with a `strategy` (see §11). Ties to 7.md's `by`/`through`/reel reconciliation.
- `append` is durable collection mutation (`rolesGranted gains the role record`, `admit a being to the roster`) — read by later flows; NOT a transient §6 bind.

## §8 — Three near-conditionals that must stay DISTINCT

| node | when the parser emits it | shape |
|---|---|---|
| `if` (§2) | inside a flow body; runs a branch **in the moment** | `{kind:"if", cond, then, else}` |
| `law` | top-level `When <state-cond>: <assertion>` — standing, definitional, **no event, no else** (`When the being holds the role at an anchor, the role is live`) | `{ kind:"law", when:<cond>, then:[{kind:"assert", clause}] }` |
| `gate` | a **pre-seal** precondition that aborts the act fail-closed (`The role exists in the registry before the grant seals`, `the real-name must not already exist`) | `{ kind:"gate", cond, phase:"before-seal", host?, onFail:{kind:"refuse", message?} }` |

Discriminator (the parser's test): top-level capitalized `When <event/op>:` → §0 flow trigger; `When <state-cond>:`/`when …,` with no actor/event → `law`; `<pred> before X seals` / `must (not) exist` → `gate`; lowercase inline `if`/`when …,` inside a body → §2 `if`.

## §9 — Type dispatch (keeps a flat surface flat)

```
{ kind: "match", on:"<ref-or-typefield>", cases:[{ when:<cond>|label, body:[<node>...] }...] }
```
- `For local fathers: … For cross-reality fathers: …` → labeled cases. `content, a shape matching its type: text matter carries…, an http carries…` → a 5-way `match`. (Forcing these into nested §2 ifs would violate the reads-cleanly rule.)

## §10 — Negation has THREE senses (disambiguate)

1. cond negation (§1): `not` / `no X` in a CONDITION → `cond.negated`.
2. act negation (§7): `a being does not own itself`, `no reveal is returned`, `records only X, never Y` → `act.negated` (+ `include`/`exclude` for `only…never`). NOT a cond.
3. absence-as-value: `accepts text or no content`, `soft-deleted` → a value member `{ absent:"content" }`, NOT a negation.

The parser routes by attachment: `not`/`no` on a verb/object → act; on a condition clause → cond; as an option in a kind's value list → absent-value.

## §11 — The LAW / declaration layer (NOT control flow, but co-surfaced — do not defer silently)

These are declarations the parser EMITS and the engine's role/capability/type
model ENFORCES (most already parse: `is`, `can`, `cannot`, `contains`,
`extends`, `has`). Listed so the two lanes don't collide when the next slices hit
them:
```
{ kind:"capability", role, polarity:"can"|"cannot", verb, of?, to?, with?, intent?, filter?, quant?, guard?:<cond>, exclusive?, passive?, host? }
{ kind:"extends", role, parent }
{ kind:"property", subject, prop, value?:<list|{oneOf:[…,{absent}]}>, optional?, closed?, gloss?, extensibleBy? }
{ kind:"prohibition", clause, negated:true }       // standalone absence law; precedence over a prior grant (rule 14)
{ kind:"derive-rule", strategy:"latest-wins", over:["grant","revoke"], by:"date", decides:{ field } }
{ kind:"lifetime", subject, until:<cond|{event}> }  // "a grant lasts until revoked"; "no wall-clock expiry"
{ kind:"inheritance-rule", subject, axis:"branch lineage", reaches:"the child", until:{ event:"<self-cancel>" } }
{ kind:"relate", subject, relation:"facet", object }            // being-to-being wiring (NAME facet of I_AM)
{ kind:"declare", subject, is:"<predicate>", article:false }    // bare "I_AM is the root"
```
Capability/prohibition carry the guard via §1 cond; **prohibition wins over a prior `can`** (precedence is the engine's, declared here).

## Open questions (engine — settle these and the contract is frozen)

1. **cond.clause depth:** is verbatim + `negated` + `subject` enough to resolve against state/binds, or pre-tokenize equality/existence/state-reads (`equals`, `qualities.father`)?
2. **break scope:** nearest `foreach` only (cherub-connect needs only this; the cascade/walk loops don't break)?
3. **refuse + return transport:** does `refuse` ride a `do:refuse`-style fact or a halt signal? Does `return` ride a fact or a transport signal? (One decision for both.)
4. **flag coreference owner:** parser (deterministic `inferFlag`, shared by mark-write/cond-read) or engine (resolve at eval)?
5. **quant firing:** edge-triggered on false→true crossing (proposed), or standing re-evaluation? Same for timer `whose … has passed` (per-tick).
6. **capability family:** stays LAW (registered, enforced by the role model, prohibition-precedence), NOT run as flow effects — confirm.
7. **stored vs derived:** parser marks `derive.stored:false` / `derive-rule` / symmetric inference; engine computes liveness/coverage/containment on READ, not persisted — confirm.
8. **gate vs law vs if discriminator:** is the §8 surface-signal test the right contract?
9. **host binding:** `{kind:"host", bind}` for a return consumed later; a `gate`/`cond` may carry `resolvedBy:<host>` (getRole, findByName, hasAuthorityOver)?
10. **trigger taxonomy completeness:** does §0's union cover every `When` head in the seven slices, with `external:true` distinguishing tick/coalesce from fact watches?

Settle these, I grow the parser to emit §0–§11, you build the caps to consume
them — the seven richest slices clear in one coordinated pass. This contract and
your engine-capability spec are the two halves of one agreement.

## Engine answers (settled — the engine lane, 2026-06-17)

Settled by the engine (a 10-way grounded fan-out, one analyst per question reading
the evaluator + each slice's JS handler; synthesis + critique folded in by hand).
All ten converge on ONE architecture; the per-question answers are framings of it.

### The unifying model — three strands, two resolution surfaces

The evaluator already separates **dry-run** (collect) from **live** (emit). The rich
slices add ONE distinction the evaluator must hold: every node belongs to exactly
one of three strands, and only the first ever lays a fact.

1. **WORLD** (facts): §7 `act` / `derive` (default) / `form-being`. The ONLY
   fact-emitters; they go through the existing `emit` / `doVerb` / `birthBeing`.
2. **CONTROL** (no facts): `if` / `foreach` / `break` / `mark` / `bind` / `match` /
   `refuse` / `return` / `gate`. Pure flow + flow-local state. `refuse` / `return`
   are TRANSPORT signals (Q3), never facts.
3. **LAW** (registered, never run): `law` / `capability` / `extends` / `property` /
   `prohibition` / `derive-rule` / `lifetime` / `inheritance-rule` / `relate` /
   `declare`, and `derive` with `stored:false`. These join the same register-only
   path the evaluator already gives `is` / `can` / `cannot` (evaluator.js L40-42);
   they are ENFORCED / COMPUTED ON READ, never executed as effects (Q6, Q7).

**The strands ARE the subject/object cut** (government-innerfold.md, 3.md objectivity).
Only the WORLD strand lays facts — because a fact is a DEED, and the chain records only
deeds. CONTROL is the forward fold's private reasoning (which branch, which item, refuse
or return): the being deciding, invisible, no facts — exactly Tabor's recall rule, *"the
reasoning is private cognition; only the two ENDS of the turn are visible."* The reel
records what was DONE, never the deliberation between. LAW is the OBJECTIVE register's
first move: the reel read backward, the pattern named, computed-on-read (authority /
liveness / containment) — `derive stored:false` is literally "objectify the reel without
adding to it." The §8 discriminator + the §16 forward-language guard are the
subject/object wall in code: deeds and private control stay in the subjective forward
fold; the law side objectifies; the because/therefore re-subjectification (RECALL, a
moment pointed backward) is the unbuilt inner register (newTest.md), and it too lays no
fact. So "only WORLD emits" is not an implementation choice — it is the chain holding
deeds and nothing else, the same law that makes recall private.

**Recall = a Name reading its OWN Word (the "being suit"), no new primitive.** Every
fact already carries its author's `nameId` (facts.js:914), so a Name's whole sequence
of Word-acts — its own Word — exists on the chain for free, labeled, persistent. The
"being suit" is that: not a private store, the Name's own slice of the public chain.
Recall is the Name reading it — a SEE scoped to its own nameId (and SEE never stamps a
fact, chainRoots.js:252, so recall stays a read, never an entry). This is the
continuity mechanism for a stateless being: an LLM-Name wakes with one fact and can't
see the last moment, so it recalls by reading its own act chain ("every Name has its
own Word"). It stays WORD (reads Word-acts) and can't drift into freeform reasoning,
because what it reads is its own chain, not an open prompt. THE FORK (Tabor's, not-built,
full note in 3.md): does the suit hold only public DEEDS (sovereignty dissolves — nothing
private to open; bad faith undetectable by design), or also a private reasoning trace
(then "who may open a suit, on what authority" is a federation rule — but it adds a
being-private store, tensioning the all-public/stateless doctrine). Either way it is the
INNER fold (unbuilt); the forward-fold caps don't change. The one primitive recall needs
— a nameId-scoped SEE — already exists.

Meaning is resolved through TWO surfaces the evaluator already seeds:

- **`getPath(base, "a.b.c", ctx)`** — a dotted accessor over `ctx.bindings` / state /
  being-qualities. The minimal extension of today's `resolveValue` (which walks only
  `$name` + `ctx.beings`). Carries the parser-lifted `test` reads/equalities (Q1).
- **`callHost(builtin, args, ctx)`** — ALREADY PRESENT (evaluator.js L214-219, the
  `act.host` escape hatch). Carries every domain-computed predicate/result (Q9).

The parser lifts **structure** (connectives, negation, operator + operand paths, the
trigger tag, the node kind); the evaluator resolves **meaning** (what a path points
at, whether a comparison/existence/host-predicate holds). The §1 split, made exact.

### The one shared resolver — `resolveCond(cond, ctx)`

`if`, `gate`, and `law` all funnel through it (one surface, three framings):
- `cond.all` / `cond.any` → recurse AND / OR.
- leaf with `test` → `getPath(subject|bind, test.path)`, apply `test.op`
  (`equals` = `idEquals(getPath, resolveValue(against))`, domain `String()==String()`;
  `reads` / `holds` = defined/truthy; `in` = membership), then XOR `cond.negated`.
- leaf with `resolvedBy` → `await callHost(resolvedBy, args, ctx)`, then XOR `negated`.
- bare leaf (no `test`, no `resolvedBy`) → read `ctx.bindings[leaf.flag]`, then XOR
  `negated`. (Sharpening of Q4: the PARSER runs `inferFlag(clause)` at parse time and
  emits the canonical `flag` name onto BOTH the `mark` node and the bare-leaf cond, so
  `inferFlag` stays entirely in the parser lane; the evaluator never re-derives it.)

### Per question (settled)

- **Q1 cond depth — LIFT THE SKELETON, NOT THE MEANING.** Leaf stays
  `{clause, negated, subject?}` PLUS an optional `test:{op, path, value?|against?}`
  the parser emits ONLY for a recognizable skeleton (equality / dotted state-read /
  existence-of-a-bind). Domain predicates (authority, ancestry, "verification
  succeeds") stay verbatim and carry `resolvedBy:<host>` instead. Verbatim-alone is
  rejected (it forces the engine to re-tokenize `equals`/`qualities.father` = parsing).
  Evidence: cherub-connect's `candidate.qualities.father.reality` + `String(a)===String(b)`
  chains (role.js connectHandler) are pure test-equalities; inheritation's
  `hasAuthorityOver` walk is the resolvedBy case — conditions are bimodal.
- **Q2 break — NEAREST `foreach` ONLY.** No multi-level break; the cascade/walk loops
  never break (they use `all:true` / accumulate). `break` is loop-exit; `refuse`/`return`
  are whole-flow unwinds (Q3) — three distinct unwinds, never conflated.
- **Q3 refuse/return — NEITHER RIDES A FACT (one decision).** `refuse` = a host HALT:
  the evaluator throws a `WordRefusal` (IbpError-shaped; `FORBIDDEN`, or a gate's
  `INVALID_INPUT`), the moment rolls back, NO fact. `return` = a SUCCESS terminator:
  resolve `values`/`extra` from bindings/flags, hand a plain `result` object to the
  host (`runRoleWord` returns `{deltaF, result}`); the transport reads it (tokens,
  `seatBranch`, `reveal`). World facts a flow lays come from §7 nodes that ran BEFORE
  the return. No `do:refuse`/`do:return` verb. Evidence: connectHandler — every
  refusal is `throw new IbpError`, every success is a `return {...}` the transport
  consumes; the be:connect fact is emitted separately by writeBeFact.
- **Q4 flag owner — PARSER, via deterministic `inferFlag(clause)`.** Shared by
  mark-write and cond-read; the evaluator only reads `ctx.bindings[flag]` and applies
  `negated`. Disjunctive accumulation (OR across sibling branches / loop passes) lives
  in how `mark` writes (set-true, never reset within a flow).
- **Q5 quant/timer firing — QUANT EDGE-TRIGGERED, TIMER PER-TICK.** A quant `when`
  fires once on the predicate crossing false→true (a per-watch `_satisfied` latch;
  reuse `pump`); a timer fires each entry past its cursor and advances `nextFireMs`
  (reuse `wakeSchedule.runOnce`'s scan; reuse `tickWheel` for `state`). Quant tally is
  computed-on-read, never a stored counter.
- **Q6 capability family — CONFIRMED LAW.** `capability` / `extends` / `property` /
  `prohibition` register (arm `registerRole` / `registerMatterType`) and are enforced
  in `authorizeViaRoles`, with prohibition-precedence; NEVER run as flow effects. Same
  register-only path as `is`/`can`/`cannot`.
- **Q7 stored vs derived — CONFIRMED COMPUTED-ON-READ.** The engine computes liveness /
  coverage / containment / authority on READ (inheritation.js `livePointsAt` L96-112,
  `hasAuthorityOver`/`authoritiesOver` L162-190 = latest-of-two-by-date over grant/revoke
  + a parent walk), never persisted. `derive` with `stored:false` emits no fact and
  writes no state — it REGISTERS a read-rule pointing at the §11 derive-rule /
  inheritance-rule. `derive` default emits exactly ONE fact via the existing emit path.
  The two forms differ purely in the `stored` flag. Only grant/revoke pairs are stored.
- **Q8 gate/law/if discriminator — ADOPTED, sharpened to HEAD-SHAPE × BODY-MOOD.**
  `When <verb-act head>:` + deed-body → §0 flow. `When <state-cond>, <assertion>.`
  (body is a MEANING, not a deed) → `law` (register-only, joins the read-fold).
  `<pred> before X seals` / `must (not) already exist` → `gate` (pre-seal, fail-closed,
  `resolvedBy:<host>`, `onFail:refuse`). lowercase inline `if` in a deed-body → §2 `if`.
  The trailing `:` (block vs comma), capitalization, and the existing §16
  forward-language guard ("a flow DOES, a conclusion MEANS") give a purely structural
  decision. Three distinct handlers: `if` runs in-moment; `gate` runs pre-seal and
  halts via refuse with no fact (mirrors do.js `authorize` L187 + nameOps declare
  conflict, both throw before `emitFact`); `law` registers only.
- **Q9 host binding — `{kind:"host", builtin, args?, bind?}` (ALREADY the `act.host`
  escape hatch); a cond/gate carries `resolvedBy:<host> + args`.** Both route through
  the ONE existing `callHost(builtin, args, ctx)`, which reads `ctx.env.host[builtin]`.
  §6 "bind from a host result" = a `host` node with `bind` (the result enters
  `ctx.bindings[bind]`, read by later nodes — the path `act.host`+`act.bind` already
  walks, evaluator.js L133-137). §9 "a cond delegates to a host predicate" = a leaf
  with `resolvedBy` (Q1's bimodal leaf); `resolveCond` calls the same `callHost` and
  XORs `negated`. The host REGISTRY (`ctx.env.host`) is populated by `runRoleWord` from
  a per-role table: the Q7 read-resolvers (`livePointsAt`, `hasAuthorityOver`,
  `authoritiesOver`), the lookups (`findByName`, `getRole`), and the matter
  `classifyMatter`. Engine work: extend `callHost` to `resolveValue` its `args`, and
  have `runRoleWord` thread `env.host`. Reconciles Q1 (`resolvedBy` is the same field),
  Q3 (`return.extra` may carry `{ref}` to a host bind), Q7 (the read-resolvers ARE host
  builtins, computed-on-read, never stored).
- **Q10 trigger taxonomy — COMPLETE, with FOUR non-trigger `When` heads peeled off
  first.** The seven tags (op / event / external-timer / state / quant / query /
  negated-guard-event) cover every TRUE trigger. The §8 discriminator runs FIRST and
  removes: the grant-role anchor head (a `law`), the two NAME-declare existence heads
  (`gate`s), the matter containment heads (`derive`). A `registerFlow` dispatcher on
  `when.kind`: op binds the be/do/name seal; event = a fact watch (`matches` + a
  `resolveCond` guard); external = the tick/coalesce loop (wakeSchedule vs
  subscriptions, `external:true` is the real seam); state = `tickWheel`; quant =
  edge-latch (Q5); query = run-and-return (Q3 + Q9).

### Two constructs the critique adds (the moment/auth boundary, from the cut)

The 10 answers cover control flow but under-specify the **moment + identity** boundary
the cherub cut already proved. Two additions, both already implemented in `runRoleWord`:

- **A flow is ONE moment; a mid-flow act throw aborts the WHOLE moment, fail-closed.**
  `refuse` (Q3) is the EXPLICIT halt; this is the IMPLICIT one — if a §7 effect
  (`doVerb`/`birthBeing`) throws partway, the already-laid facts are on the shared
  `deltaF` and the moment must NOT half-seal. The rule the cut enforces (role.js
  `_birthViaWordOrJs`: facts laid + a throw → rethrow, never a partial fallback) is the
  general law: **a flow's effects are atomic in the moment.** No node for it; it is the
  evaluator's try/seal contract. Add to §7.
- **A flow's effects run under a DECLARED acting identity (by / through), not the
  summoner's.** The §0 `op` trigger binds `actor` (and `alias`); the effects authorize
  AS that actor. cherub.word's acts are "by I_AM, through Cherub" — `runRoleWord`
  builds a derived summonCtx (identity + `actorAct.nameId` = the actor; `beingId` = the
  vessel) over the real chain. Generalize: a flow declares its acting Name (`by`) and
  optional vessel (`through`); the bridge threads them into the derived summonCtx, so
  authorize sees the right identity and facts attribute correctly (7.md's by/through).
  Without this, every converted role's acts would authorize as the wrong identity.

### New evalNode cases — build order by leverage

1. **`if`** (Q8) + `resolveCond` + `getPath` — every rich slice. The keystone.
2. **`mark`** + bare-leaf flag read (Q4) — pervasive; flow-local OR-accumulator.
3. **`host`** / `bind` (Q9, §6) — extend the existing `act.host`; thread `env.host`.
4. **`foreach`** + **`break`** (Q2) — nearest-loop; `all:true` and `walk` sources.
5. **`refuse`** + **`return`** (Q3) — transport signals; `WordRefusal` / `{deltaF,result}`.
6. **`gate`** (Q8) — pre-seal, fail-closed, `resolvedBy` host; reuses the refuse case.
7. **`derive`** (Q7) — `stored:false` registers a read-rule; default emits one fact.
8. **`match`** (§9) — labeled/cond cases; lowest leverage (could defer; collapses to
   nested `if` if pressed, but the 5-way matter-type dispatch earns it).
9. **`law`** + the §11 declarations (Q6) — register-only; ONE branch shared with
   `is`/`can`/`cannot` (an `isDeclaration(kind)` set). `assemble`/`notify`/`transition`
   are SUGAR over `act`/`derive`/`summon`, not new capabilities.

### Contract edits 8.md needs (parser lane)

- **§0:** the evaluator reads `flow.effects` today; the contract says `flow.body`.
  RECONCILE to `body` (rename in `evalFlow`), or the parser emits `body`. Pick `body`.
- **§1:** add the `test:{op, path, value?|against?}` triple and the `resolvedBy:<host>,
  args` alternative to the leaf (the bimodal cond).
- **§6/§9:** unify — `host` node and cond `resolvedBy` both name `ctx.env.host[builtin]`
  through `callHost`; drop any second host shape.
- **§7:** state the atomic-moment rule + the flow's acting identity (by/through).
- The seven slices all clear under this set; the governance INNER FOLD (the reflective
  because/therefore layer) is correctly OUT of scope here — it is newTest.md, kept out
  by the §16 forward-language guard, not this contract.
