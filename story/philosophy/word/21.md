# 21 — The final pathway: Word built out of Word

_Reconciled from five surveys (TARGET doctrine 9/17/20.md + rasterize/llm; word-engine; stamper/inner-face; verbs/dispatch; doc-series), then adversarially re-verified against the live code. Evidence is file:line. This is the dependency-ordered path from here to 20.md's end-state: Word built out of Word down to a kernel, chain-native (loop=fold / state=fold / control=conditional-Word / PC=chain-head), native-words-via-matter, compile-Word, one-act-per-word._

## TRUE current standing (VERIFIED against `/home/tabor/github/Tree/story` code)

### Built (verified)
- **IS-BE on the wire.** SEE is the inert noun-read (`see.js:210-215` fold-only dispatch, resolves from word-fold with no Map fallback; `maybeAutoGrantOnEntry` deleted per 17.md); the stamp is the one act; call = stamp + await. (17.md:137-148)
- **Presentism, structurally enforced.** An Act row exists only when cognition returned `ok:true` (`stamper/4-stamped.js:16`, `1-assign.js:16`). The word is the moment's trace. (20.md:144)
- **Rasterized inner face, built.** `buildInnerFace` assembles one face per moment from being/space/matter reels (`stamper/2-fold/innerFace.js:149-212`, `foldPlace.js:89-152`); `weave.js` audits the reels read. rasterize.md:59-62 is real.
- **State = fold of facts.** `foldEngine.js` (hot, cold rebuild), `foldAt.js` (pure historical), reducers `(state,fact)->state`. No mutation. Per-aggregate reels, `p` prev-hash chain, reelHeads.
- **word.word root + boot-fold wired** (9.md Phase 1+2 LANDED). Root exists (`seed/store/words/word.word`); `genesis.js:277-301` runs `seedFold` after `ensureIAm` (line 297) and **before** `ensureSpaceRoot` (line 306), then `rehydrateWordProjection('0')` (line 285). The word-fold does not depend on the place-fold.
- **A word IS a fold of coin/retire facts**, not a registry (`wordStore.js:1-14`): `bindWord` lays a coin fact, `_projection` is the live sync fold, `getWordSync` drives dispatch (`:152-155`). Bedrock guard (`:37-42/:79/:97`) protects I_AM's `'0'` words; per-history shadowing preserved.
- **One resolver under four verbs** (`resolveWordFromFold`, `wordStore.js:181-210` = 17.md STEP 1); `emitWordFact` unified emitter (`factResult.js:55-88` = STEP 2); four BE emits collapsed (`be.js:450/502/557/602` = STEP 4, byte-identical).
- **do-op dispatch is FOLD-ONLY, no Map fallback** (`do.js:70-85`, throws on miss `:80-85`). _Verified: the `wordStore.js:214` comment "only on a Map miss" is STALE — the live do.js has no fallback path._
- **`seedFold` declares all nine word-kinds in one boot call** (`wordFold.js:119-145`): verb pasts + 20 concept .words + ops/types/reducers/able-words/name-ops/be-ops/see-ops.
- **Parser + evaluator working.** Template-matcher (`parser.js`, 25+ RULES + EFFECT_RULES, cherub birth, state machines, inline-if, foreach). `evalCall` (`evaluator.js:465-498`) and `evalRecall` (`:326-381`) **exist and run with live implementations** — _verified: word-engine survey's "not implemented" is outdated._
- **Live bridge** (`ableWordRegistry`): fold-first resolve, live IR run.
- **Book/Library (18/19.md)** — the 5D library reel, withNameAct, share/receive, genesis book; **materials symmetry (15.md)**; **shape+IR doctrine (1/4/5.md, clause grain locked).**

### Half-finished (THE PATHWAY STARTS HERE — Tabor flagged these as "only half finished")
- **BE connect+birth** still use `writeBeFact` (`be.js:662,756`) while release/switch/death/truename moved to `emitWordFact`. _VERIFIED structural: be.js:747-754 documents that birth bypasses writeBeFact because cherub's `birthBeing` already stamps `be:birth` with the full spec (homeSpace/defaultAble/parentBeingId); a second emit would duplicate and the reducer would clobber the just-set state._ So P0 is real coordination work, not a flip.
- **NAME write-path** resolved to fold (`name.js:100` calls `resolveNameOpFromFold`) but still hand-emits via `writeNameFact` (`:127-134`, impl `:148-170`). Adopt emitWordFact keystone after birth+connect land.
- **DO-op .word sweep** — `portal.word` proven (`_factParams`), ~1 of ~75 converted. Remaining ~74 in JS.
- **Flow/type/reducer .words** — `harmony.word`/`sun.word` run live; ~16 flows + ~20 kind decls remain in JS.
- **Parser -> recursive descent** — `parser.js:1-27` self-describes as larval; 56 gap-forms mapped (no precedence/associativity/error-recovery).
- **Law strand** — collected into `ctx.laws` (`evaluator.js:103-117`), never evaluated as a gate.
- **SEE authority predicates** — parsed; host population at `ableWordRegistry.js:299` incomplete.
- **9.md Phase 3-5** — boot-fold wired (Phase 1+2), but `DECLARED_PAST` not asserted-equal to word.word's fold; no axiom anchor documented; no descent test; no boot symmetry guard (STEP 7).

### Dead / superseded
- Five dispatch Maps as dispatch truth (now registration buffers only, `do.js:77`, `wordStore.js:237-250`).
- `maybeAutoGrantOnEntry` side-fact (deleted; SEE is a pure noun-read).
- In-memory reel cache (stubs).
- `symbol`/`lexicon`, materials-merge, able-as-4th-primitive, wall-clock-in-substrate, verses-as-AST, curly-brace syntax, the 4-resolver fork.

### Not started (doctrine end-state, grep-confirmed zero code)
- **Native-words-via-matter** (matter-hash + runtime-tag + effect-class; tag→executor; WASM sandbox) — 20.md:113-129.
- **Compile-Word** (resolve→inline→lower→kernel-ops; signed compiled Book; cache-by-hash) — 20.md:84-90.
- **Chain-native loop/control-flow words** — `evalForeach` exists in JS but NO `if.word`/`while.word`/`for.word` — 20.md:27-40.
- STEP 6/7/8, conformance suite, word-in/word-out cognition (14.md), keystroke-as-act (llm.md), emergent language, merge/concurrent-writers (13.md), federation precedence (17.md STEP 0), Rust host, snapshot/per-able reels.

## The corrected final pathway

**P0 — Finish the half-folded verb dispatch (BE connect+birth, NAME write-path).** _Goal:_ every WRITE path emits through the unified keystone except do (principled audit) and call (wire). _Problem:_ birth's path is complex — it bypasses writeBeFact (be.js:747-754) to avoid clobbering homeSpace/defaultAble set by birthBeing, so connect.word/birth.word must coordinate with birthBeing + cherub's handler (gate order + result shape matter). _Lands:_ 17.md STEP 4 completion + STEP 5. _Work:_ write `connect.word`/`birth.word` (return factParams, gate-check, careful resolution order), migrate both to `emitWordFact`; adopt keystone in name.js, delete `writeNameFact`. _Depends on:_ nothing. _Gate:_ `verify-connect-cut`/`-live` + `verify-birth-word` + `verify-name-ops-fold` green; cross-reality-attribution-e2e 8/8 unchanged; byte-diff identical. _Coordination:_ **17.md agent owns be.js/name.js/birthBeing** — this is theirs; the .word lane supplies the files.

> **UPDATE 2026-06-21 — the result-contract seam is RESOLVED (the P0 unblock).** Routing NAME through `emitWordFact` risked stamping the freshly-minted private key onto the immutable chain (`writeNameFact` stamps NO result; the keystone stamped `result:stripForAudit(result)`, and `reveal` isn't in `REVEAL_KEYS`). FIX landed on the keystone (`factResult.js`, engine lane): a per-kind result policy — (1) `verb:"name"` → **omit result** (hard floor, key-safe regardless of field names); (2) `binding.resultPolicy.keep:[…]` → curate (connect keeps `{beingAddress,note}`); (3) else `stripForAudit` (the 4 existing be-ops unchanged). **Gate: `verify-keystone-result` 3/3** — a real private key in the result does NOT reach the chain; be-ops 6/6 / cherub-cut 9/9 / name-act 4/4 no regress. The dispatch swap is now UNBLOCKED + key-safe: NAME auto-safe; connect sets `resultPolicy`; birth routes `birthBeing`'s single stamp through the keystone.

**P1 — Close 9.md self-hosting (source-of-truth + axiom anchor + descent verify).** _STATUS: LANDED 2026-06-21 (engine lane) — verify-descent 4/4, kernel==word.word, 12 axioms / 8 theorems._ _Goal:_ word.word is the SOURCE of the runtime tense/verb/foundation projections; the AXIOM/THEOREM line is documented beside word.word; the boot asserts kernel==word.word; a descent test proves the surface self-describes. _Lands:_ 9.md Phase 3/4/5. _Work:_ derive `DECLARED_PAST` from the word.word fold or assert parity (`verbTense.js`); add the axiom anchor (name host-implemented axioms vs composite theorems); add the boot symmetry guard (STEP 7, soft→hard); write the descent test. _Depends on:_ P0 (complete seed fold). _Gate:_ new `verify-descent.mjs`; `verify-word-fold-boot` green. _Coordination:_ **Claude/engine lane** (9-10 doctrine). _Note:_ Phase 1+2 already landed (genesis.js:277-301 verified).

**P2 — Convert the DO-op / flow / type backlog to .words (toward zero in-world JS).** _Goal:_ ~74 do-ops, ~16 flows/schedules, ~20 kind/reducer decls become fold facts; each JS site deleted as its slice verifies; REGISTRY Maps demoted to pure bootstrap buffers everywhere. _Lands:_ 2.md Phase 5. _Work:_ convert do-ops batch-by-batch following `portal.word` (delete each JS handler after byte-diff); migrate flows following `harmony.word`/`sun.word`; author the kind/reducer .words (bodies behind refs); delete the REGISTRY Map after full coverage. _Depends on:_ P1 (seed fold complete, descent proven). _Gate:_ per-slice `verify-*-cut` byte-identical; full e2e green; leak-grep proving no dispatch path reads a Map. _Coordination:_ **parser/.word agent owns the sweep**; Claude supplies fold machinery + verifiers.

**P3 — Recursive-descent parser + wire the law strand + SEE predicates.** _Goal:_ the template-matcher becomes a real grammar (precedence, associativity, error recovery, receiver/quantifier/plural); the law strand evaluates as gate conditions; SEE authority predicates fully wired into `ctx.env.host`. _Lands:_ Parser Phase 3 (8.md); law as the OBJECTIVE register; SEE = pure noun-read with real predicate backing. _Work:_ replace RULES/EFFECT_RULES with a recursive-descent grammar (keep round-trip on harmony/sun/cherub); wire `ctx.laws`→`authorizeViaAbles`/`registerMatterType`; populate `ctx.env.host` (hasAuthorityOver/isAncestorOf/findByName) at `runAbleWord` (`ableWordRegistry.js:299`). _Depends on:_ P2 (the .word corpus forces the gaps). _Gate:_ `verify-statement` + `verify-controlflow-parse` + `verify-cond` at the new grammar; new `verify-law-gate.mjs` (a cannot-law refuses a do); cherub-connect ancestor walk resolves via the wired predicate. _Coordination:_ **parser/.word agent owns parser.js**.

**P4 — Chain-native control-flow words (loop/state/if as folds).** _Goal:_ `if.word`/`while.word`/`for.word` exist as conditional Words whose execution is a fold growing the chain: head=program counter, condition=see (inert), body=stamp; state is the fold; a paused program is a chain head; resume = lay the next fact. _Lands:_ 20.md chain-native substitution; the is-be blade one level up from 17.md (each iteration = a see then a stamp). _Work:_ author the three control words; lower the JS `evalForeach` into the chain-native loop-word form (condition is a see-read of the fold, body is one stamp per iteration); prove a paused loop = a chain head, resume = lay next fact, replayable via verifyReel. _Depends on:_ P3 (grammar + cond surface real). _Gate:_ new `verify-chainloop.mjs` (each iteration one fact, replay byte-identical, pause/resume, head=PC); `verify-flow` stays green. _Coordination:_ joint (.word authors the words, Claude wires the lowering).

**P5 — Native-words-via-matter (binding + matter type + effect-class).** _Goal:_ a word's fold binding can carry a matter-hash (content-addressed blob) + matter **type** (wasm/js/native/model…) + effect-class (pure/effectful); dispatch fetches the matter by hash and runs it through that matter type's **run-op** (the executor); pure words are replay-safe, effectful are fact-sources. (Tabor: "resource types" are just **matter types** — a model/code/wasm is matter of a type, not a separate category; the type's run-op IS the executor. No "extension," no "resource" — only Word + typed Matter.) _Lands:_ 20.md native-words — the kernel stops being a privileged lump and becomes "the words whose matter is native," in the same fold. WASM is the sweet spot (deterministic, sandboxed, portable). _Work:_ extend `bindWord`/`resolveWordFromFold` to carry matterHash + runtimeTag + effectClass; add a tag→executor driver registry (wasm first, sandboxed); fetch matter by hash from CAS, marshal Word inputs across, stamp the result; enforce the effect-class gate. _Depends on:_ P1 (axiom anchor — the native floor IS the axiom set) + P2 (the fold is the sole dispatch truth). _Gate:_ new `verify-matterword.mjs` (a wasm-tagged pure word runs sandboxed, cacheable-by-hash, replay-identical; an effectful word stamps once and is not recomputed). _Coordination:_ **Claude/engine lane (kernel)**.

> **FOUNDATION + REGISTRY-FOLD LANDED 2026-06-21 (engine lane).** `seed/present/word/matterWord.js` — the run-op handler layer + effect-class cache: `registerDriver(type, fn)` registers a matter type's run-op handler; a sandboxed **wasm** handler (`{}` imports = no ambient authority → pure words stay replay-safe) + a marked-less-safe **js** handler; `runMatterWord(matter, inputs, {getContent, getMatterType})` resolves executability + effect-class FROM the matter-type registry (the source of truth), then caches pure / never-caches effectful. **The driver registry is folded into the matter-type registry** (Tabor's realization, done): `materials/matter/types.js` gains an `executable:{effect,entry}` field, `wasm`+`js` are registered as executable matter types, and the field RIDES THE FOLD (`declareTypesToFold`/`resolveTypeFromFold` carry it) — so the production fold-first `getMatterType` sees a type as runnable. It is the same name/handler split do-ops use (the type declares it runs + its effect-class; the executor is registered separately), NOT a parallel truth table; a non-executable type refuses. **Gates: `verify-matterword` 6/6** (wasm sandboxed, effect from the type, pure caches, effectful override, non-executable refuses, errors) + **`verify-typesfold` 4/4** (executable rides the fold). **ENGINE HALF OF THE DISPATCH PAIRING LANDED 2026-06-21:** `matterWord.js` gains the production entries — `storeMatterBody(blob, type)` (putContent → a CAS ref), `resolveMatterWord(name)` (does a word's binding carry a `matter:{hash,type}` body? — a pure matter-bodied word has no `do.ref`, so it resolves HERE, not via resolveWordFromFold), and `runWordBody(matterRef, inputs)` (the entry the dispatch calls — wired to the REAL `contentStore.getContent` + `getMatterType`). `bindWord` already carries the matter ref (the binding is open). **Gate: `verify-matterword-cas` 4/4** (store a wasm body in the real CAS → bind it as a word → resolve → run = 42, effect "pure" from the live registry; a non-matter word resolves null). DISPATCH (paired, done): the EFFECTFUL/do half is wired (verb lane — matter rides `op.matter` through do.js's auth + auto-Fact; `verify-matterword-dispatch` 5/5). The PURE/see half has its engine runner — **`seeMatterWord(name, inputs)`** (the peer of runWordBody-via-do): a pure matter word computes on the see path and stamps NO fact; an effectful one is REFUSED (must go via DO); a non-matter word → `{isMatter:false}` (fall through). **The effect-class IS the dispatch branch** (the is-be cut one level down). Gate: `verify-matterword-cas` 7/7 (pure see-computes, 0 facts; effectful refused). REMAINING (verb lane): wire the see-dispatch to `seeMatterWord` (a one-liner now). Plus two co-design follow-ups: wasm structured-input marshalling (fails loud today) + the final pure-skip is then closed.

**P6 — Compile-Word (resolve → inline → lower → content-addressed compiled Book).** _Goal:_ a one-time compiler resolves each word against the fold, inlines compositions into flat routines, lowers control-flow into a fixed sequence of kernel primitive ops (hash, persist, fold-step, compare, branch), and emits a signed content-addressed compiled Book; execution skips parse/resolve and runs the precomputed ops; cache-by-hash forever. _Lands:_ 20.md compile-Word — "do the deciding once," inlining = the compression idea at machine level, the compiled output is itself a Book; this is what makes self-hosting fast (collapse the tower of interpreters). _Work:_ build the resolve/inline/lower pipeline; make the compiled output a signed content-addressed Book, reproducible (same source + same compiler → same hash); self-host the compiler in Word once the kernel-op floor and matter-words (P5) exist. _Depends on:_ P4 (control-flow-as-Word is what gets lowered) + P5 (kernel-op floor / matter-backed executors are the target). _Gate:_ new `verify-compileword.mjs` (deterministic compiled-Book hash; compiled run byte-identical to interpreted; recompiling the same Word hits cache). _Coordination:_ **Claude/engine lane (kernel)**.

**P7 — Freeze the conformance suite; the end-state seals.** _Goal:_ a frozen corpus of .word programs with expected facts/folds/descriptors defines the world's behavior; any host (incl. a future Rust kernel) must pass it. With self-hosting (P1), native-words (P5) and compile (P6) landed, the only host left is the irreducible kernel floor. _Lands:_ 2.md Phase 6 + 20.md end-state (Word built out of Word; bootstraps itself forever after the first host-seed ignition). _Work:_ assemble + freeze the corpus (pin rule-14 prohibition precedence, NAME/world split, escape determinism); declare the AXIOM kernel set frozen (crypto/CAS/raw-IO/minimal evaluator — the bottom turtle, documented and tiny); mark the seam (everything above the kernel is .word, unfoldable by construction). _Depends on:_ P6. _Gate:_ new `verify-conformance.mjs` — THE gate any host must pass.

## The honest seams

- **The bottom turtle stays host.** Crypto (Blake3), CAS, raw IO, the minimal evaluator stay kernel — you can't hash with a thing that needs hashing. The end-state is "no JS _above_ the kernel." P1's axiom anchor names, freezes, and shrinks that floor. P5 native-words-via-matter generalizes it into the fold but does NOT eliminate it: matter-backed words are _trust_ boundaries, not _verify_ boundaries (matter can do IO/randomness/mutation).
- **The compile/perf seam is real.** A Word evaluator written in Word interpreting Word is a tower of interpreters and crawls. P6 (compile, not interpret-all-the-way-up) is the only escape, hard-gated on P5's kernel-op floor. Attempting P6 before P5 yields a self-hosted-but-unusably-slow system.
- **The "fold-only" doctrine is ahead of its comments.** `do.js:70-85` has no Map fallback, but `wordStore.js:214` still says "only on a Map miss." Don't trust a comment over the live verb.
- **Two evaluator stubs were wrongly reported missing.** `evalCall`/`evalRecall` exist with live verifiers — do not re-build them.
- **Lane coordination is load-bearing.** The 17.md agent owns do.js/be.js/see.js/name.js + the parser/.word sweep; the Claude/engine lane owns 9-10 doctrine, the fold machinery (wordStore/wordFold), and the kernel floor (P5-P7). **P0-P4 live mostly in the 17.md / parser lanes; P1 and P5-P7 are the engine lane.** Mis-assignment stalls both.
- **STEP 3 (do) and STEP 8 (call) are principled exemptions.** do's audit target is dynamic (resolveAuditTarget + summarizeAuditResult); call is irreducible as WIRE (async delivery + receiver-gate + response channel). Forcing either into the fixed-noun `emitWordFact` shape is the "weird word rule" 17.md forbids.
- **Birth's half-finished state is structural, not oversight.** be.js:747-754: birthBeing already stamps be:birth with the full spec; a second writeBeFact emits a duplicate the reducer clobbers. P0 coordinates with birthBeing — it is not a one-line flip.
