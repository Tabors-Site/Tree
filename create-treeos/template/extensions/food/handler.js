/**
 * No pre-processing needed. The AI in the mode handles everything.
 * Food-log mode parses and writes data via tools.
 * Food-coach mode handles setup and advice.
 * The orchestrator's suffix routing picks the right mode.
 */

export async function handleMessage() {
  return null;
}
