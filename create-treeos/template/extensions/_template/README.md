# Extension Template

Copy this directory and rename it to get started.

1. Rename the directory to your extension name (lowercase, hyphens)
2. Update `manifest.js`: set name, version, description, and declare your dependencies
3. Implement `init(core)` in `index.js`
4. Restart the server

See `EXTENSION_FORMAT.md` in the extensions directory for the full reference.
