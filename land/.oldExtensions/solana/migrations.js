/**
 * Solana extension migrations.
 * Migration 1 already ran (moved wallet data from old versions[].wallet to metadata.solana.wallets).
 * No further migrations needed after schema flatten.
 */
export default [];
