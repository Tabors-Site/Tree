{
  "name": "treeos-portal",
  "private": true,
  "version": "0.2.0",
  "description": "TreeOS portal — an IBP client hosting four views (3d, text, console, explorer) over one state",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "socket.io-client": "^4.7.5",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "vite": "^6.0.1"
  }
}
